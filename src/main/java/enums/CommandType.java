package enums;

/**
 * Enum representing the various command types used in the Backgammon game.
 */
public enum CommandType {
    ROLL,
    QUIT,
    HINT,
    PIP,
    END_MATCH,
    DOUBLE,
    ACCEPT,
    REFUSE,
    DICE,
    TEST,
    END_GAME
}